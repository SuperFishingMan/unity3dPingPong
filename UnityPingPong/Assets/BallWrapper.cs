using UnityEngine;
using System.Collections;

public class BallWrapper : MonoBehaviour {

	// Use this for initialization
	void Start () {
		// rigidbody.AddForce( new Vector3(0f,3f,-5f ) );
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
